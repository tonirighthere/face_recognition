PyQt5>=5.15.9
opencv-python>=4.8.0
mediapipe>=0.10.0            # Thay thế YOLOv8 để detect mặt thuần CPU
insightface>=0.7.3
onnxruntime>=1.16.0          # Hoặc onnxruntime-gpu nếu muốn chạy InsightFace trên GPU
numpy>=1.24.0
gradio>=4.0.0

# Lưu ý:
# - ultralytics (YOLOv8) đã được thay thế bằng mediapipe để tối ưu hiệu năng trên CPU.
#   Nếu vẫn cần YOLOv8 cho mục đích khác, cài thêm: ultralytics>=8.0.0
